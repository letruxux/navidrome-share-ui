{
  "name": "navidrome-share-ui",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "docker:build": "docker buildx build --platform linux/amd64,linux/arm64 -t ghcr.io/letruxux/navidrome-share-ui:latest .",
    "docker:publish": "docker buildx build --platform linux/amd64,linux/arm64 -t ghcr.io/letruxux/navidrome-share-ui:latest --push .",
    "docker:build:local": "docker build -t navidrome-share-ui ."
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@elysiajs/static": "^1.2.1",
    "cheerio": "^1.2.0",
    "elysia": "^1.4.29"
  }
}
